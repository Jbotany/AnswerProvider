{{ form_start(form) }}
<div class="row mt-3">
    <div class="col-12">
        {{ form_label(form.label, 'Titre de la question') }}
        <div> {{ form_widget(form.label) }} </div>
{#        <div class="alert alert-danger">{{ form_errors(form.label) }}</div>#}
    </div>
    <div class="col" id="some-custom-id">
        {# {{ form_row(form.dueDate) }} #}
    </div>
</div>
{{ form_end(form) }}
